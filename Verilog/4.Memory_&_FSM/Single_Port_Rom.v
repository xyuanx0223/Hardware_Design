// Matlab Code
// t = 0: 0.001:1;
// x = 5 * sin(2 * pi * 50 * t ) + 5;
// plot(t,x);
// Y = cast(x,'uint8');
// Hd = dfilt.dffir(Y);
// Hd.Arithmetic = 'fixed';
// Hd.CoeffWordLength = 8;
// coewrite(Hd, 10, 'mycoefile');
